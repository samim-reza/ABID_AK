# ABID AK — SSL nginx config. Activated by deploy.sh --ssl once certs exist.

upstream abidak_frontend { server frontend:3000; }
upstream abidak_backend  { server backend:8000; }

# HTTP -> HTTPS redirect (keeps ACME challenge reachable for renewals)
server {
    listen 80;
    listen [::]:80;
    server_name samimreza.me www.samimreza.me;

    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name samimreza.me www.samimreza.me;
    client_max_body_size 25m;

    ssl_certificate     /etc/letsencrypt/live/samimreza.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/samimreza.me/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    add_header Strict-Transport-Security "max-age=31536000" always;

    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;

    location /api/           { proxy_pass http://abidak_backend; }
    location = /health       { proxy_pass http://abidak_backend; }
    location /docs           { proxy_pass http://abidak_backend; }
    location /redoc          { proxy_pass http://abidak_backend; }
    location = /openapi.json { proxy_pass http://abidak_backend; }

    location / {
        proxy_pass http://abidak_frontend;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
