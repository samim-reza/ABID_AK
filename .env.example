# Root deployment configuration used by docker-compose.
# The public URL where the app is reachable (used to bake the frontend API base).
PUBLIC_URL=https://samimreza.me

# Domain + email for Let's Encrypt certificate issuance (deploy.sh).
DOMAIN=samimreza.me
CERTBOT_EMAIL=admin@samimreza.me
